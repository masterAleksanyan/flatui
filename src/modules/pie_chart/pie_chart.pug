mixin pie_chart(el, p, b)
  div(id=""+el class="donut-size")
    .pie-wrapper
      span.label
        span.num 0
      .pie
        .left-side.half-circle
        .right-side.half-circle
      .shadow
  script.
    updateDonutChart('#{el}', #{p}, #{b})