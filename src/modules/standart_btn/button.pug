mixin button(options)
  - var bs = "st-btn--"+options.size
  - var rg = "st-btn--"+options.color
    button(class="st-btn ripple-elem "+bs +" "+rg +" "+(options.myClass ? options.myClass : ""))= options.text